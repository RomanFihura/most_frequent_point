#include "most_frequent_point.h"

int main(int argc, char *argv[])
{
    return 0;
}
// a.emplace_back();
// a.back().push_back(2);
// a.back().push_back(4);
// a.emplace_back();
// a.back().push_back(1);
// a.back().push_back(3);
// a.emplace_back();
// a.back().push_back(3);
// a.back().push_back(10);