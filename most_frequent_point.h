#pragma once

#include <algorithm>
#include <random>
#include <unordered_map>
#include <vector>

uint32_t random_coordinate(uint32_t max);
uint32_t mostFrequent(std::vector<std::vector<uint32_t>> a);
